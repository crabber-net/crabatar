from .context import <<[.cus.package.name.python]>>
import pytest

class TestModels:
    def test_sample_class(self):
        assert 'pigs' != 'flying'
        sample_instance = <<[.cus.package.name.python]>>.SampleClass
        assert sample_instance is not None
